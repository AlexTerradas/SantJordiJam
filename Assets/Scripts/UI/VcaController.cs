using UnityEngine;
//using FMOD.Studio;
using UnityEngine.UI;

public class VcaController : MonoBehaviour
{
    /*private VCA Controller;
    public string VcaName;

    private Slider slider;

    void Start()
    {
        float startingVolume = PlayerPrefs.GetFloat(VcaName + "Volume", 0.5f);
        Controller = FMODUnity.RuntimeManager.GetVCA("vca:/" + VcaName);
        Controller.setVolume(startingVolume);
        slider = GetComponent<Slider>();
        slider.value = startingVolume * 10;
    }

    public void SetVolume(float newVolume)
    {
        Controller.setVolume(newVolume/10);
        PlayerPrefs.SetFloat(VcaName + "Volume", newVolume/10);
    }

    public void IncreaseVolume()
    {
        slider.value += 1;
        SetVolume(slider.value);
    }

    public void DecreaseVolume()
    {
        slider.value -= 1;
        SetVolume(slider.value);
    }*/
}